---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="天汇科技 · 企业宣传"
  description="轻量化企业官网示例：产品、团队与联系方式一站式展示。"
>
  <header class="top">
    <div class="wrap top__inner">
      <a class="logo" href="/">天汇<span>科技</span></a>
      <nav class="nav">
        <a href="#about">关于我们</a>
        <a href="#services">业务</a>
        <a href="#contact">联系</a>
      </nav>
      <a class="btn btn--ghost" href="#contact">预约沟通</a>
    </div>
  </header>

  <main>
    <section class="hero wrap">
      <div class="hero__copy">
        <p class="eyebrow">轻量化 · 高性能 · 易维护</p>
        <h1>用清晰叙事，传递可信赖的企业形象</h1>
        <p class="lede">
          基于 Astro 的静态站点：首屏快、结构简单、部署成本低，适合企业宣传、活动落地与品牌官网。
        </p>
        <div class="hero__actions">
          <a class="btn btn--primary" href="#services">了解业务</a>
          <a class="btn btn--outline" href="#contact">获取方案</a>
        </div>
        <ul class="metrics">
          <li><strong>99.9%</strong><span>可用性目标</span></li>
          <li><strong>&lt; 1s</strong><span>首屏体验（静态部署）</span></li>
          <li><strong>0</strong><span>默认运行时 JS 负担</span></li>
        </ul>
      </div>
      <div class="hero__panel" aria-hidden="true">
        <div class="card">
          <h3>今日要点</h3>
          <ul>
            <li>统一品牌语气与视觉层级</li>
            <li>突出核心业务与差异化</li>
            <li>明确的行动召唤（CTA）</li>
          </ul>
        </div>
      </div>
    </section>

    <section id="about" class="section wrap">
      <h2 class="section__title">关于我们</h2>
      <p class="section__intro">
        我们帮助成长型企业用更少的复杂度完成对外表达：文案精炼、版式克制、技术栈轻量，让访客快速理解「你是谁、你能做什么、如何联系你」。
      </p>
      <div class="grid-3">
        <article class="tile">
          <h3>专注</h3>
          <p>围绕核心产品线组织信息，避免堆砌模块，降低维护成本。</p>
        </article>
        <article class="tile">
          <h3>可信</h3>
          <p>用事实与案例说话，配合合规展示资质与客户背书。</p>
        </article>
        <article class="tile">
          <h3>可扩展</h3>
          <p>后续可接入博客、招聘或表单，而不推翻现有结构。</p>
        </article>
      </div>
    </section>

    <section id="services" class="section section--alt">
      <div class="wrap">
        <h2 class="section__title">核心业务</h2>
        <div class="grid-2">
          <article class="feature">
            <h3>品牌与官网建设</h3>
            <p>信息架构、视觉规范与落地页制作，交付可迭代的组件化页面。</p>
          </article>
          <article class="feature">
            <h3>内容运营支持</h3>
            <p>新闻稿、案例故事与多语言版本协作，保持对外口径一致。</p>
          </article>
          <article class="feature">
            <h3>技术与部署</h3>
            <p>静态托管、CDN 与基础监控建议，控制总拥有成本。</p>
          </article>
          <article class="feature">
            <h3>数据与线索</h3>
            <p>轻量表单与统计对接，便于市场团队跟进商机。</p>
          </article>
        </div>
      </div>
    </section>

    <section id="contact" class="section wrap contact">
      <div class="contact__card">
        <h2 class="section__title">联系我们</h2>
        <p class="section__intro">
          留下你的需求简述，我们会在 1–2 个工作日内回复。以下内容仅为示例，请替换为真实信息。
        </p>
        <dl class="contact__list">
          <div>
            <dt>邮箱</dt>
            <dd><a href="mailto:hello@example.com">hello@example.com</a></dd>
          </div>
          <div>
            <dt>地址</dt>
            <dd>中国 · 某市某区某路 88 号</dd>
          </div>
        </dl>
        <a class="btn btn--primary" href="mailto:hello@example.com">发送邮件</a>
      </div>
    </section>
  </main>

  <footer class="footer">
    <div class="wrap footer__inner">
      <span>© {new Date().getFullYear()} 天汇科技 · 保留所有权利</span>
      <span class="footer__note">由 Astro 构建</span>
    </div>
  </footer>
</BaseLayout>

<style>
  .wrap {
    width: min(1120px, calc(100% - 40px));
    margin-inline: auto;
  }

  .top {
    position: sticky;
    top: 0;
    z-index: 10;
    background: color-mix(in srgb, var(--bg) 88%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border);
  }

  .top__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 14px 0;
  }

  .logo {
    font-weight: 700;
    letter-spacing: 0.02em;
  }

  .logo span {
    color: var(--brand);
  }

  .nav {
    display: flex;
    gap: 22px;
    font-size: 0.95rem;
    color: var(--muted);
  }

  .nav a:hover {
    color: var(--text);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 18px;
    border-radius: 999px;
    font-weight: 600;
    font-size: 0.95rem;
    border: 1px solid transparent;
    transition:
      background 0.15s ease,
      color 0.15s ease,
      border-color 0.15s ease,
      box-shadow 0.15s ease;
  }

  .btn--primary {
    background: var(--brand);
    color: #fff;
    box-shadow: 0 8px 24px rgba(29, 78, 216, 0.25);
  }

  .btn--primary:hover {
    background: #1e40af;
  }

  .btn--outline {
    border-color: var(--border);
    background: var(--surface);
    color: var(--text);
  }

  .btn--outline:hover {
    border-color: #cbd5f5;
  }

  .btn--ghost {
    border-color: transparent;
    background: transparent;
    color: var(--brand);
  }

  .btn--ghost:hover {
    background: var(--brand-soft);
  }

  .hero {
    display: grid;
    gap: 40px;
    padding: 64px 0 48px;
    align-items: start;
  }

  @media (min-width: 900px) {
    .hero {
      grid-template-columns: 1.1fr 0.9fr;
      padding: 88px 0 64px;
    }
  }

  .eyebrow {
    display: inline-block;
    padding: 6px 12px;
    border-radius: 999px;
    background: var(--brand-soft);
    color: #1e3a8a;
    font-weight: 600;
    font-size: 0.85rem;
    margin: 0 0 16px;
  }

  .hero h1 {
    font-size: clamp(2rem, 4vw, 2.75rem);
    line-height: 1.15;
    margin: 0 0 16px;
    letter-spacing: -0.02em;
  }

  .lede {
    margin: 0 0 28px;
    color: var(--muted);
    font-size: 1.05rem;
    max-width: 52ch;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 28px;
  }

  .metrics {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 16px;
  }

  .metrics li {
    padding: 14px 16px;
    border-radius: var(--radius);
    background: var(--surface);
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
  }

  .metrics strong {
    display: block;
    font-size: 1.25rem;
    margin-bottom: 4px;
  }

  .metrics span {
    font-size: 0.85rem;
    color: var(--muted);
  }

  .hero__panel .card {
    background: var(--surface);
    border-radius: 16px;
    padding: 24px;
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
  }

  .hero__panel h3 {
    margin: 0 0 12px;
    font-size: 1.1rem;
  }

  .hero__panel ul {
    margin: 0;
    padding-left: 18px;
    color: var(--muted);
  }

  .hero__panel li + li {
    margin-top: 8px;
  }

  .section {
    padding: 64px 0;
  }

  .section--alt {
    background: #eef2ff;
    border-block: 1px solid #e0e7ff;
  }

  .section__title {
    margin: 0 0 12px;
    font-size: 1.75rem;
    letter-spacing: -0.02em;
  }

  .section__intro {
    margin: 0 0 32px;
    color: var(--muted);
    max-width: 62ch;
  }

  .grid-3 {
    display: grid;
    gap: 18px;
  }

  @media (min-width: 768px) {
    .grid-3 {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .tile {
    background: var(--surface);
    border-radius: var(--radius);
    padding: 22px;
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
  }

  .tile h3 {
    margin: 0 0 8px;
    font-size: 1.1rem;
  }

  .tile p {
    margin: 0;
    color: var(--muted);
    font-size: 0.95rem;
  }

  .grid-2 {
    display: grid;
    gap: 18px;
  }

  @media (min-width: 768px) {
    .grid-2 {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .feature {
    background: var(--surface);
    border-radius: var(--radius);
    padding: 22px;
    border: 1px solid #c7d2fe;
  }

  .feature h3 {
    margin: 0 0 8px;
  }

  .feature p {
    margin: 0;
    color: var(--muted);
  }

  .contact {
    padding-bottom: 80px;
  }

  .contact__card {
    background: var(--surface);
    border-radius: 18px;
    padding: 32px;
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
    max-width: 640px;
  }

  .contact__list {
    margin: 0 0 24px;
  }

  .contact__list > div {
    margin-bottom: 14px;
  }

  .contact__list dt {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--muted);
  }

  .contact__list dd {
    margin: 4px 0 0;
    font-weight: 500;
  }

  .footer {
    border-top: 1px solid var(--border);
    padding: 22px 0 32px;
    color: var(--muted);
    font-size: 0.9rem;
  }

  .footer__inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 10px;
  }

  .footer__note {
    opacity: 0.85;
  }

  @media (max-width: 720px) {
    .nav {
      display: none;
    }
  }
</style>
